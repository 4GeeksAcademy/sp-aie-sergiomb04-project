[project]
name = "trackflow-services-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi==0.116.1",
  "uvicorn==0.35.0",
  "python-multipart==0.0.20",
  "tinydb==4.8.2",
]

[project.scripts]
seed = "trackflow_api.seed:main"
