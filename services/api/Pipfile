[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[packages]
fastapi = "==0.116.1"
uvicorn = "==0.35.0"
python-multipart = "==0.0.20"

[dev-packages]
httpx = "==0.28.1"

[requires]
python_version = "3.12"

[scripts]
dev = "uvicorn incidents_api.app:app --reload --host 0.0.0.0 --port 8000"
start = "uvicorn incidents_api.app:app --host 0.0.0.0 --port 8000"
